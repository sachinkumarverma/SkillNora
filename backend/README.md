Backend API (Express)

Run locally:

```
cd backend
npm install
npm run dev
```

API base: `http://localhost:4000/api/*`

Features are organized under `src/features/<feature>` with `routes.js`, `controller.js`, `service.js`.
